# Log

## [2026-06-28] ingest | リバースプロキシとは.md

ソース要約ページを作成。新規作成したページ: `source_リバースプロキシとは`, `リバースプロキシ`, `CORS`, `nginx`。`index.md` を初期化。

## [2026-06-28] ingest | aws_configureとは.md

ソース要約ページを作成。新規作成したページ: `source_aws_configureとは`, `aws_configure`。`index.md` を更新。
